import React from 'react';
import ReactDOM from 'react-dom';

if (usingReactApp !== "undefined") {
    ReactDOM.render(
        <h1>Hello, world!</h1>,
        document.getElementById('root'),
    );
}