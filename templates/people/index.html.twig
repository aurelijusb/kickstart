{% extends 'base.html.twig' %}

{% block title %}Hello PeopleController!{% endblock %}

{% block body %}
    <div id="root">Loading...</div>
    <noscript>To use this app, enable JavaScript</noscript>
    <div>
        Versijuojamas JavaScript failas: <b id="versionedFile">...</b>
    </div>

    Vardas:
    <input class="my-2" type="text" id="name"/>
    <span id="validation-name-result" data-path="{{ path('validate', {'element': 'name'}) }}"></span><br/>
    Komanda:
    <input type="text" id="team"/>
    <span id="validation-team-result" data-path="{{ path('validate', {'element': 'team'}) }}"></span>
{% endblock %}

{% block javascripts %}
    <script>
        var usingVersionedFileJs = true;
        var usingReactApp = true;
    </script>
    {{ encore_entry_script_tags('validation') }}
    {{ parent() }}
{% endblock %}
