{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
  <style>
    .example-wrapper {
      margin: 1em auto;
      max-width: 800px;
      width: 95%;
      font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
      background: #F5F5F5;
      padding: 2px 6px;
    }
  </style>

  <div class="example-wrapper">
    <h1>Validate! ✅</h1>

    <div>
      Name: <input type="text" id="name"/>
      <span id="validation-result-name" data-path="{{ path('validateStudent', {'element': 'name'}) }}"></span>

    </div>
    <div>
      Team: <input type="text" id="team"/>
      <span id="validation-result-team" data-path="{{ path('validateStudent', {'element': 'team'}) }}"></span>
    </div>

  </div>
{% endblock %}

{% block javascripts %}
  <script src="{{ asset('build/js/validateStudent.js') }}"></script>
  {{ parent() }}
{% endblock %}
